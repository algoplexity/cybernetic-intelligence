
---

# **KGRAG Analyst Co-Pilot: Zero-Knowledge Handover**
"You've never seen this project. In 30 minutes, you'll own it."
**Location:** SharePoint > "Compliance Analyst Co-Pilot" Folder

### **1. The 3 Folders That Run Everything**

Your entire project lives in these three folders. **Never delete them.**

| Folder Name | What It Is | Status |
| :--- | :--- | :--- |
| **`1_Source_Code/`** | All the Python scripts (`.py` files) that make the system work. This is the "Engine Room." | **READ-ONLY MASTER** |
| **`2_Model_Assets/`** | The final, trained AI models. These are the system's "brains." They are generated by the scripts in `1_Source_Code`. | **DO NOT EDIT** |
| **`3_Data_And_Reports/`** | All the input data (CSVs), the massive Knowledge Graph, and the final output reports. | **Working Directory** |

### **2. How to Open the Data (3 Clicks)**

*(This is inspired by the simplicity of your original reference document.)*

1.  **Install (once):** `pip install pandas pyarrow`
2.  **Open any file (copy-paste):**
    ```python
    import pandas as pd
    # Example for the main report
    df = pd.read_csv("..\\3_Data_And_Reports\\primary_analytical_report.csv")
    print(df.head())
    ```
*No SQL. No cloud. Just Python + Excel.*

### **3. The Golden Rule (Never, Ever Break This)**

**NEVER EDIT THE SCRIPTS IN THE `1_Source_Code/` FOLDER.**

This folder is our only "master" copy. Before you make *any* changes, you **MUST** create a personal copy (e.g., `1_Source_Code_MyDevBranch_YYYY-MM-DD/`) and work only in that new folder.

### **4. How The Analyst Co-Pilot Is Built (Copy-Paste This)**

This is the entire analysis engine in a nutshell. It shows how the final trained "Brain" is used to answer questions.

```python
# The entire analysis engine.
# 1. Load the final trained model ("The Brain") and the Knowledge Graph
#    (These paths point to the official asset folders)
model = torch.load("..\\2_Model_Assets\\gfm_retriever_production_v1\\gfm_retriever_final.pth")
graph = torch.load("..\\3_Data_And_Reports\\production_kg_v1\\production_graph.pt")
metadata = json.load(open("..\\3_Data_And_Reports\\production_kg_v1\\metadata.json"))

# 2. Ask a question
#    (The answer_query_rich function is inside final_query_validation.py)
results = answer_query_rich("Show me examples of supplier audits.")

# 3. Get the answer
print(results)
```
*Run this code (from within your personal copy of `1_Source_Code/`) -> you have the official analysis.*

---
