
---
# **KGRAG Analyst Co-Pilot: Zero-Knowledge Handover**
"You've never seen this project. In 30 minutes, you'll own it."

### **1. The 3 Folders That Run Everything**
Your entire project lives in these three folders.

| Folder Name | What It Is | Status |
| :--- | :--- | :--- |
| **`Code/`** | Contains the single master notebook (`master_pipeline.ipynb`) that runs the entire project. | **READ-ONLY MASTER** |
| **`Models/`** | The final, trained AI "brains" of the system. | **Generated by Code** |
| **`Data/`** | All input, intermediate, and final data. | **Generated by Code** |

### **2. The Master Pipeline**
The entire project is contained in the **`Code/master_pipeline.ipynb`** notebook. It is a single file divided into sequential stages. Run the cells in order to execute the full pipeline from data download to final analysis.

### **3. The Golden Rule**
**NEVER EDIT THE MASTER NOTEBOOK.** Before you make changes, **make a personal copy** (e.g., `master_pipeline_MyDev.ipynb`) and work only in that copy.

### **4. How The Analyst Co-Pilot Works**
The final cells of the master notebook show you how to use the system. Here is the core logic:

```python
# 1. Load the final trained model and graph from their asset folders
model.load_state_dict(torch.load(MODEL_FINAL_PATH))
graph = torch.load(os.path.join(KG_ASSET_PATH, "graph.pt"))

# 2. Ask a question
results = answer_query_rich("Show me examples of supplier audits.")
```
*Run the final cell in `master_pipeline.ipynb` -> you have the official analysis.*
---
---

